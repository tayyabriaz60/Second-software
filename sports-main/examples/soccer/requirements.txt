ultralytics
gdown
# GPU RF-DETR (RunPod): install ONE matched stack — do not mix cu12 wheels with a
# CUDA-13 onnxruntime-gpu build (error: libcublasLt.so.13 not found).
#
# Option A — keep existing nvidia-*-cu12 wheels:
#   pip install -U 'onnxruntime-gpu==1.20.1'
# Option B — keep newest ORT (needs CUDA 13):
#   pip install -U onnxruntime-gpu nvidia-cublas-cu13 nvidia-cudnn-cu13 \
#     nvidia-cuda-runtime-cu13 nvidia-cuda-nvrtc-cu13 nvidia-cufft-cu13 \
#     nvidia-curand-cu13 nvidia-cusolver-cu13 nvidia-cusparse-cu13 nvidia-nvjitlink-cu13
onnxruntime-gpu>=1.20.1